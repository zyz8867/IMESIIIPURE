<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="popmenu_animation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/popup_fade_in</item>
        <item name="android:windowExitAnimation">@anim/popup_fade_out</item>
    </style>

    <style name="map_option_popup" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/trans_up</item>
        <item name="android:windowExitAnimation">@anim/trans_down</item>
    </style>

    <style name="bottom_pop">
        <item name="android:windowEnterAnimation">@anim/pop_out</item>
        <item name="android:windowExitAnimation">@anim/pop_back</item>
    </style>

    <style name="answer_sheet_haul">
        <item name="android:windowEnterAnimation">@anim/sheet_in</item>
        <item name="android:windowExitAnimation">@anim/sheet_out</item>
    </style>

    <style name="map_info_container">
        <item name="android:windowEnterAnimation">@anim/bottom_in</item>
        <item name="android:windowExitAnimation">@anim/bottom_out</item>
    </style>

    <style name="AppTheme.splash_theme">//必须继承activity相关的theme
        <item name="android:windowBackground">@drawable/splash_bkg</item>//设置背景图片
        <item name="android:windowFullscreen">true</item>
        //全屏显示，隐藏statusBar
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <style name="rb_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:button">@null</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
